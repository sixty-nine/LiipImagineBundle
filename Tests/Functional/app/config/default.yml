imports:
    - { resource: framework.yml }

parameters:
    liip_imagine.web_root: %kernel.root_dir%/web

liip_imagine:
    filter_sets:
        my_thumb:
            quality: 75
            filters:
                thumbnail: { size: [200, 200], mode: inset }
